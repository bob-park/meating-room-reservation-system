# server
server:
  port: 8761

# spring
spring:
  application:
    name: discovery-service

# eureka
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

## logging
logging:
  level:
    root: info
    com:
      m2rs: debug